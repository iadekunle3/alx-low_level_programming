#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strlen_recursion - is the string length
 * @s: is the string pointer
 * Return: as specified
 */
int _strlen_recursion(char *s)
{
	return strlen(s);
}

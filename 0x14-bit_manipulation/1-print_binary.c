#include "main.h"
/**
 * print_binary - is used to print binary to numbers
 * @n: is the user input
 * Return: as specified
 */
void print_binary(unsigned long int n)
{
	if (b > 1)
		binary_to_unit(b >> 1);
	_putchar((b & 1) + '0');
}

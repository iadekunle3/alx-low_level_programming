#include <stdio.h>

/**
 * main - Prints numbers between 0 to 9.
 * Description: This is a count from 0 to 9
 * Return: Always 0 (positive mindset)
 */
int main(void)
{
	int i;

	for (i = 48; i < 58; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
